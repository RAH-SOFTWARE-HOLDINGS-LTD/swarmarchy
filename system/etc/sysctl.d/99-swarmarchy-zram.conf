# Tuning for zram-backed swap (see default/systemd/zram-generator.conf).
# zram is RAM-fast and random-access, so bias the kernel toward using it and
# drop swap read-ahead (which only helps rotational/disk swap).
vm.swappiness = 100
vm.page-cluster = 0
