# Swarmarchy — Sway window rules (translated from default/hypr/windows.conf + apps/*)
# Sway uses `for_window [criteria] command`. xdg-shell apps match on app_id,
# XWayland apps match on class.

# Keep idle inhibited while any window is fullscreen
for_window [class=".*"] inhibit_idle fullscreen
for_window [app_id=".*"] inhibit_idle fullscreen

# Default window opacity (hypr: opacity 0.97 on the default-opacity tag).
# Sway applies opacity per-window; .97 across the board mirrors Swarmarchy.
for_window [app_id=".*"] opacity 0.97
for_window [class=".*"] opacity 0.97

# Picture-in-Picture: float + sticky (translated from default/hypr/apps/pip.conf)
for_window [title="^Picture-in-Picture$"] floating enable, sticky enable
for_window [app_id="^.*pip.*$"] floating enable, sticky enable

# Common floating utilities
for_window [app_id="^(blueman-manager|nm-connection-editor|pavucontrol)$"] floating enable
for_window [app_id="^org.gnome.Calculator$"] floating enable
for_window [window_role="pop-up"] floating enable
for_window [window_role="bubble"] floating enable

# TODO(port): default/hypr/apps/*.conf has many more app-specific rules
#   (steam, telegram, qemu, localsend, jetbrains, webcam-overlay, etc.).
#   Translate the ones you actually use from class/title -> app_id/class here.
