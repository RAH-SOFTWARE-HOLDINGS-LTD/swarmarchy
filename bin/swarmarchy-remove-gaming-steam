#!/bin/bash

# swarmarchy:summary=Remove Steam and all of its game libraries, configs, and caches.
# swarmarchy:requires-sudo=true

set -e

swarmarchy-pkg-drop steam

rm -rf \
  "$HOME/.steam" \
  "$HOME/.local/share/Steam" \
  "$HOME/.config/steam" \
  "$HOME/.cache/steam"

echo ""
echo "Steam and its data have been removed."
