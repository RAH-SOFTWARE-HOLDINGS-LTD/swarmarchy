#!/bin/bash

# swarmarchy:summary=Launch a TUI command in the default terminal with Swarmarchy styling
# swarmarchy:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.swarmarchy.$(basename $1) -e "$1" "${@:2}"
