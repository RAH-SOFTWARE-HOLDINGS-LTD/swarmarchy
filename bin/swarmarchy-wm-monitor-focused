#!/bin/bash

# swarmarchy:summary=Print the name of the currently focused monitor.
# (Ported to Sway; filename retained for compatibility.)

swaymsg -t get_outputs | jq -r '.[] | select(.focused == true).name'
