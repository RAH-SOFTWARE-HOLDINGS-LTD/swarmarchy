#!/bin/bash

# swarmarchy:summary=Print the detected touchpad or trackpad device name
# (Ported to Sway; filename retained for compatibility.)

device=$(swaymsg -t get_inputs | jq -r '[.[] | select(.type == "touchpad") | .name] | first // empty')
[[ -n $device ]] && echo "$device"
