#!/bin/bash

# swarmarchy:summary=Launch a TUI or focus an existing terminal window for it
# swarmarchy:args=<command> [args...]

APP_ID="org.swarmarchy.$(basename "$1")"
LAUNCH_COMMAND="swarmarchy-launch-tui $@"

exec swarmarchy-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
