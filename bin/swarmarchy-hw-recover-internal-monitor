#!/bin/bash

# swarmarchy:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/swarmarchy/toggles/hypr/internal-monitor-disable.conf"

if [[ -f $TOGGLE ]] && ! swarmarchy-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
