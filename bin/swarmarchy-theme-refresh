#!/bin/bash

# swarmarchy:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/swarmarchy/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  SWARMARCHY_THEME_SKIP_BACKGROUND=1 swarmarchy-theme-set "$(cat $THEME_NAME_PATH)"
fi
