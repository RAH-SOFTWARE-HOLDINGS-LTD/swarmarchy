#!/bin/bash

# omarchy:summary=Restart the nightlight (wlsunset) if it is currently running.
# (Ported to Sway: hyprsunset -> wlsunset. Nightlight is user-toggled, not themed.)

if pgrep -x wlsunset >/dev/null; then
  pkill -x wlsunset
  setsid wlsunset -t 4000 -T 4001 >/dev/null 2>&1 &
fi
