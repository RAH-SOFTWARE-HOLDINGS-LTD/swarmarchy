#!/bin/bash

# swarmarchy:summary=Overwrite the user config for the lock screen (swaylock) with the Swarmarchy default.
# (Refreshes ~/.config/swaylock/config from the Swarmarchy default.)

swarmarchy-refresh-config swaylock/config
