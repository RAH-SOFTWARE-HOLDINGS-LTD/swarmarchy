#!/bin/bash

# omarchy:summary=Restart the swayidle service (used for idle detection and auto-lock).
# (Ported to Sway; filename retained for compatibility.)

pkill -x swayidle
omarchy-swayidle >/dev/null 2>&1 &
